spring.application.name=pet-service
server.port=8082

# Configuración Flexible: Usa la variable de Docker o localhost por defecto
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/mascotas_db
spring.datasource.username=postgres
spring.datasource.password=1234

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Kafka: Flexible para local o contenedor
spring.kafka.bootstrap-servers=${KAFKA_HOST:localhost}:9092

# Seguridad
jwt.secret=sanosysalvos1234